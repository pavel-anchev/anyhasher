name: Continuous Integration
on: [push]
jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - run: pwd
      - run: ls
      - run: pwd
      - run: npm i
      - run: npm run build
